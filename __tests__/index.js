/* eslint-disable no-undef */

'use strict'

const lib = require('../lib/index.js')


describe('lib', () => {
  // Работает логгирование
  // Все запросы мимо urlSection отдают 404
  // Все запрсосы на urlSection + url соответствуют ответу request
})

describe('bind', () => {
  // Правильно обрабатываются параметры
  // Правильно обрабатывает ситуацию с занятым портом
  // С правильными параметрами сервер стартует и обрабатывает запросы
})
